name: Test

on:
  push:

jobs:
  test:
    runs-on: [observer-self-hosted]
    container: rust:latest  # pre-installed Rust, cargo, gcc — nothing to install
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run tests
        run: cargo test
