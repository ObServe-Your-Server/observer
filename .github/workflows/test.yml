name: Test

on:
  push:

jobs:
  test:
    name: Run tests
    runs-on: [observe-self-hosted]
    container: rust:latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Run tests
        run: cargo test --workspace
