name: Test
on:
  push:
  workflow_call:

jobs:
  test:
    name: Run tests
    runs-on: self-hosted
    container: rust:latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Run tests
        run: cargo test --workspace
